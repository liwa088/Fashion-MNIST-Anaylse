# Fashion-MNIST-Anaylse
This notebook trains and evaluates a neural network on the Fashion-MNIST dataset using PyTorch. It includes data loading, preprocessing, model building, training, accuracy evaluation, and visualizations of predictions. The project demonstrates a complete deep-learning workflow for image classification
